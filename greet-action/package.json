{
  "name": "greet-action",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --format=cjs --target node --outfile=dist/index.cjs --sourcemap"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "shared": "workspace:*"
  }
}
